// datatypes

var companyName="LuminarTechnolab";// string

// typeof(object) return data type
console.log(typeof(companyName));

var employeeCount=120; //integer
console.log(typeof(employeeCount));

var isOpen=true; //bool
console.log(typeof(isOpen));

var rating=5.0;//float
console.log(typeof(rating));

// string, number, boolean, null, undefined

let isActive;//undefined

let isOk=null;



// Arithmetic(+,-,/,*,%, ** )

// logical operator (and,or,not) (&&(and),||(logical or),!(not))

// relational (<,>,<=,>=,==,!=)

// Assignment(=,+=,-=,*=)

// bitwise operator(&,|,^)